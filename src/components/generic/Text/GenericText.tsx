import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const GenericText = ({children}) => {
  return <Text>GenericText</Text>;
};

export default GenericText;

const styles = StyleSheet.create({});
